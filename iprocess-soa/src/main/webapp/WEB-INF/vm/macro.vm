#set($orderType= {0:"全部",1:"订购",2:"自动续费",3:"手动续费",4:"升级"})
#set($periodType= {-1:"试用一个月",1:"一个月",3:"一季",6:"半年",12:"一年",9999:"永久"})
#macro (showPage $module $page)
    #if($page)
        #set($pagePos = 2) ##当前页的尾巴长度
    <span class="text">共$!{page.totalItem}条记录</span>
    <span class="text">共$!{page.totalPage}页</span>
        #if($page.previousPageAvailable) ##如果能向上翻页
        <a class="prev" href="$module.addQueryData("page",$!{page.previousPage}).render()">上一页<b></b></a>
        <a class="prev" href="$module.addQueryData("page",1).render()">首页</a>
        #else
        <span class="prev-disabled">上一页<b></b></span>
        <span class="prev-disabled">首页</span>
        #end
        #if($!page.index>$pagePos+2) <span class="text">...</span> #end ##显示前面的省略号
        #foreach($i in [$pagePos..1]) ##增加前面的尾巴
            #if($!page.index - $i > 1)
        #set($pageIndex= $!page.index - $i)
    <a href="$module.addQueryData("page",$pageIndex).render()">$pageIndex</a>
    #end
    #end
    <a class="current">$!page.index</a> ##显示当前页号
        #foreach($i in [1..$pagePos]) ##增加后面的尾巴
            #if($!page.totalPage - $!page.index - $i > 0)
        #set($pageIndex= $!page.index + $!i)
    <a href="$module.addQueryData("page",$pageIndex).render()">$pageIndex</a>
    #end
    #end
        #if($!page.totalPage - $!page.index > $pagePos+1) <span class="text">...</span> #end ##显示后面的省略号
        #if($page.nextPageAvailable) ##显示后面的页
        <a class="next" href="$module.addQueryData("page",$!{page.totalPage}).render()">末页</a>
    <a class="next" href="$module.addQueryData("page",$!{page.nextPage}).render()">下一页<b></b></a>
    #else
    <span class="next-disabled">末页</span>
    <span class="next-disabled">下一页<b></b></span>
    #end
    <input name="totalPage" type="hidden" value="$!{page.totalPage}"/>
    <span class="txt">到<input class="input" name="toPage" type="text" size="2"/>页&nbsp;&nbsp;&nbsp;&nbsp;
        <input class="btn-vice" type="button" value="确定" onclick="goToPage('$module.addQueryData("page",'-page-').render()')"
               style="cursor:pointer;"></span>
        $!module.reset()
    #end
#end

##操作类型
#macro(showOperateType $operateType)
    #if("$operateType" == "renew")续费#elseif("$operateType" == "upgrade")升级#{else}订购#end
#end

##获取周期名称
#macro(getPeriodName $period)
    #if($!period == -1)试用一个月
    #elseif($!period == 1)一个月
    #elseif($!period == 3)一个季度
    #elseif($!period == 6)半年
    #elseif($!period == 12)一年
    #elseif($!period == 9999)永久
    #else
    #end
#end
##只输出一个错误
#macro(showSimpleError)
    #if(!$!actionErrors.empty)
        #foreach($str in $actionErrors)
            $!str
            #break
        #end
    #end
#end
##输出当前页面所有错误
#macro(showError)
    #if(!$!actionErrors.empty)
        #foreach($str in $actionErrors)
        <li>$!str</li>
        #end
    #end
#end
##只输出一个错误信息
#macro(showSimpleMessage)
    #if(!$!actionMessages.empty)
        #foreach($str in $actionMessages)
            $!str
            #break
        #end
    #end
#end
##输出当前页面所有错误信息
#macro(showMessage)
    #if(!$!actionMessages.empty)
        #foreach($str in $actionMessages)
        <li>$!str</li>
        #end
    #end
#end

#macro (showSimpleFieldError $field)
    #if($fieldErrors && $fieldErrors.get($field) && $fieldErrors.get($field).size() > 0)
        $fieldErrors.get($field).get(0)
    #end
#end

#macro (showFieldError $field)
    #if($fieldErrors && $fieldErrors.get($field) && $fieldErrors.get($field).size() > 0)
        #foreach($error in $fieldErrors.get($field))
        <li>$error</li>
        #end
    #end
#end


#macro(showText $id $map)
    #if($!id != 0)
        $!map.get($!id)
    #end
#end


##积分类型
#macro(showOrderType $type)
    #if($type==1) 后付
    #elseif($type==2) 在线支付
    #else 未知
    #end
#end

##营销活动状态，2==未开始，4==已结束，发放时间当天显示进行中，过去之后就显示已结束
#macro(marketingStatusDesc $status $startTime)
    #if($status == 4)
    发送完成
    #elseif($status == 2)
    待发送
    #elseif($status == 1)
    已删除
    #elseif($status == 3)
    待发送
    #end
#end

##展示店铺等级的文案
#macro(showShopLevel $value)
    #if($value==1)一星会员
    #elseif($value==2)二星会员
    #elseif($value==3)三星会员
    #elseif($value==4)四星会员
    #elseif($value==5)五星会员
    #end
#end

##展示会员店铺等级的option
#macro(showShopLevelStatePer $selectValue)
    #foreach($i in [1,2,3,4,5])
    <option value="$i" #if($i==$selectValue) selected="selected" #end>#showShopLevel($i)</option>
    #end
#end
